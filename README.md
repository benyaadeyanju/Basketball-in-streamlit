# Basketball-in-streamlit

# All the following libaries must be install before running the program

pip install streamlit 
pip install pandas 
pip install pybase64 
pip install  matplotlib
pip install seaborn
pip install numpy 
